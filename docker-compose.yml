services:
  postgres:
    image: postgres:16-alpine
    container_name: releaseops-postgres
    environment:
      POSTGRES_DB: releaseops
      POSTGRES_USER: releaseops
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-releaseops_secret}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U releaseops -d releaseops"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  backend:
    build: ./backend
    container_name: releaseops-backend
    ports:
      - "127.0.0.1:3001:3001"
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql://releaseops:${POSTGRES_PASSWORD:-releaseops_secret}@postgres:5432/releaseops
    volumes:
      - backend-sessions:/app/sessions
      - backend-logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  frontend:
    build: ./frontend
    container_name: releaseops-frontend
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      backend:
        condition: service_healthy
    volumes:
      - ./certs:/etc/nginx/certs:ro
    restart: unless-stopped

volumes:
  pgdata:
  backend-sessions:
  backend-logs:
